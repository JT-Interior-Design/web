import React from 'react';
import g from 'glamorous';

export default ({ className }) => (
  <div>
    <div className={`${className}`}>
      <h1>JASON TAYLOR</h1>
    </div>
    <p>INTERIORS</p>
  </div>
);
